# Codex plan

This folder is a repo-level guide for Codex so improvements stay consistent.

## Priorities

### P0
- No-secret config (env vars)
- Works without ElevenLabs (print-only fallback)
- Tests + CI

### P1
- Package refactor with a stable entrypoint

### P2
- Intent router + CLI

## Commands

```bash
pip install -r requirements-dev.txt -r Becky_Assistant_v1.0/requirements.txt
ruff check .
pytest
```
